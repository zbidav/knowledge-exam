# dependencies
node_modules/

# build output
dist/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# editor / OS
.vscode/
.idea/
.DS_Store
*.swp

# project scratch (per shared-server hygiene: keep scratch out of git)
tmp/

# ---------------------------------------------------------------------------
# SOURCE MATERIAL — never committed to the PUBLIC repo.
# These are the uploaded past-exam PDFs and David's personal study summaries
# (copyrighted / personal). Only the DERIVED text (content/**.xml question
# decks written from them) is committed. Keep the originals local-only.
# ---------------------------------------------------------------------------
Prev_exams/
source_material/
*.pdf
